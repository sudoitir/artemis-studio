# Copy to .env next to this file and fill in. Used by compose.prod.yaml.

# Database
DB_NAME=artemis_studio
DB_USER=artemis_studio
DB_PASSWORD=change-me

# Encrypts broker credentials at rest (AES-256-GCM, ADR-0009).
# MUST be base64 of exactly 32 bytes or the app will not start.
# Generate: openssl rand -base64 32
SECRET_KEY=change-me

# Image to run. :dev is the moving pre-stable tag; pin a CalVer tag for real
# deployments, e.g. sudoit1/artemis-studio:2026.09.0
STUDIO_IMAGE=sudoit1/artemis-studio:dev

# Where to publish the UI. Keep it on localhost and put a TLS proxy in front.
STUDIO_BIND=127.0.0.1:8080

JAVA_OPTS=-XX:MaxRAMPercentage=75

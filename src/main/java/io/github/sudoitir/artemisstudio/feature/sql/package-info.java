/**
 * SQL over queues, the message index and message capture.
 */
@ApplicationModule(displayName = "SQL console")
package io.github.sudoitir.artemisstudio.feature.sql;

import org.springframework.modulith.ApplicationModule;

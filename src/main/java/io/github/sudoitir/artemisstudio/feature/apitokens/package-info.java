/**
 * Bearer tokens for automation and MCP clients.
 */
@ApplicationModule(displayName = "API tokens")
package io.github.sudoitir.artemisstudio.feature.apitokens;

import org.springframework.modulith.ApplicationModule;

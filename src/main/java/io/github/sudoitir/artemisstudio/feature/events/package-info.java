/**
 * Broker notification history and its live topic.
 */
@ApplicationModule(displayName = "Broker events")
package io.github.sudoitir.artemisstudio.feature.events;

import org.springframework.modulith.ApplicationModule;
